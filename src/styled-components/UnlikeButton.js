import { styled } from "@mui/material/styles";
import FavoriteBorderIcon from "@mui/icons-material/FavoriteBorder";

const UnlikeButton = styled(FavoriteBorderIcon)(() => ({
  width: "15px",
  height: "15px",
  fill: "#fff",
}));

export default UnlikeButton;
